# Release Notes (v0.1.0)

**Highlights**
- Project structure established for Arduino Pro Micro firmware
- Versioned snapshot folder (`versions/v0.1.0/`)
- Experiments area for quick prototyping

**Next**
- Move stable sources from `src/` into this folder when the first build is ready
- Tag the commit with `v0.1.0` and publish a GitHub Release
